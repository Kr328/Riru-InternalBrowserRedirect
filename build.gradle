buildscript {
    repositories {
        google()
        jcenter()
        maven { url "file:///home/null/Workspace/JavaProjects/rirutools/build/repo/" }
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.3.2'
        classpath 'com.github.kr328:riru-gradle-tools:1.0-alpha03'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
